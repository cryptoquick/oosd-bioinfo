//============================================================================
// Name        : File.cpp
// Author      : Andrew Leitheiser
// Version     :
// Copyright   : Your copyright notice
// Description : Hello World in C++, Ansi-style
//============================================================================

#include <iostream>
#include <fstream>
#include <string>
using namespace std;

class File {
	string path;
protected:
	string data;
public:
	void load();
	File(string p) { path = p; }
	void print() { cout << data; }
};

void File::load() {
	string output = "";
	string line;
	int i = 0;
	ifstream input ("../fasta_data/M90848dentist.fasta");
	if(!input.is_open()) {
		cout << "Failed to open file " << path;
		return;
	}

	while(! input.eof()) {
		getline(input, line);
		if(i != 0) {
			output += line;
		}
		i++;
	}
	cout << output;

	input.close();
	data = output;
}


int main() {

	File blah("../fasta_data/M90848dentist.fasta");
	blah.load();
	blah.print();

	return 0;
}
